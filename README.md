# API REST com Flask
 Exemplo de API REST em Flask com relacionamento entre tabelas na ORM SQLAlchemy.

## Instalação
Para execução do projeto foi utilizado a versão do `Python 3.11`.
Também é necessário realizar execução do comando abaixo para instalar as dependencias do projeto:

    pip install -r requirements.txt
